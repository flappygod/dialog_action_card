import 'package:flutter/cupertino.dart';

/// ios 类键盘弹出的动画时间
const Duration iosDefaultDuration = Duration(milliseconds: 480);

/// ios 类键盘弹出非线形动画
const Curve iosDefaultCurve = Cubic(0.34, 0.84, 0.12, 1.00);
